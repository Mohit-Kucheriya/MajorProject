<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WanderLust</title>
  </head>
  <body>
    <h3>Details for particular list</h3>
    <ul>
      <li><%= listing.title %></li>
      <li><%= listing.description %></li>
      <li>&#x20B9; <%= listing.price.toLocaleString("en-in") %></li>   <!--to add coma(,) in rupees we use toLocaleString("en-in")-->
      <li><%= listing.location %></li>
      <li><%= listing.country %></li>
    </ul>
    <a href="/listings/<%= listing._id %>/edit">Edit your Listing</a>
  </body>
</html>
